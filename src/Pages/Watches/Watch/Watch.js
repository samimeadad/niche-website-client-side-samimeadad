import { Box, Typography } from '@mui/material';
import React from 'react';

const Watch = () => {
    return (
        <Box>
            <Typography variant="h2">Watch Cards Here</Typography>
        </Box>
    );
};

export default Watch;